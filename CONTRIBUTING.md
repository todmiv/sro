# Правила участия в проекте НОСО

Мы рады приветствовать вклад в развитие проекта НОСО! Этот документ описывает правила и рекомендации для участников.

## 📋 Содержание

- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Требования к коду](#требования-к-коду)
- [Стиль кода](#стиль-кода)
- [Тестирование](#тестирование)
- [Документация](#документация)

## 🤝 Как внести вклад

### 1. Подготовка

- Ознакомьтесь с [README.md](README.md)
- Проверьте открытые [Issues](https://github.com/todmiv/sro/issues)
- Создайте Issue для обсуждения изменений, если его еще нет

### 2. Разработка

1. **Форкните репозиторий** на GitHub
2. **Создайте ветку** для вашей работы:
   ```bash
   git checkout -b feature/AmazingFeature
   # или
   git checkout -b fix/BugFix
   # или
   git checkout -b docs/UpdateDocumentation
   ```
3. **Внесите изменения** согласно требованиям
4. **Протестируйте** свои изменения
5. **Зафиксируйте изменения** с понятным сообщением:
   ```bash
   git commit -m "feat: добавить новую функцию калькулятора"
   ```

### 3. Pull Request

1. **Обновите вашу ветку** с основной веткой:
   ```bash
   git checkout main
   git pull origin main
   git checkout your-branch
   git rebase main
   ```
2. **Отправьте изменения** в ваш форк:
   ```bash
   git push origin your-branch
   ```
3. **Создайте Pull Request** на GitHub
4. **Опишите изменения** подробно в описании PR

## 🔄 Процесс разработки

### Типы коммитов

Мы используем [Conventional Commits](https://conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление или исправление тестов
- `chore:` - обслуживание (обновление зависимостей и т.д.)

### Примеры сообщений коммитов

```bash
feat: добавить валидацию формы заявки
fix: исправить расчет компенсационного фонда
docs: обновить инструкцию по установке
style: исправить отступы в компоненте Header
refactor: оптимизировать компонент Calculator
test: добавить тесты для формы регистрации
```

## 📝 Требования к коду

### Общие требования

- Код должен быть читаемым и понятным
- Избегайте сложных конструкций без необходимости
- Добавляйте комментарии для сложной логики
- Используйте осмысленные названия переменных и функций

### React компоненты

- Используйте функциональные компоненты с хуками
- Добавляйте PropTypes или TypeScript типы
- Разделяйте компоненты на небольшие, переиспользуемые части
- Используйте кастомные хуки для повторяющейся логики

### JavaScript/TypeScript

- Используйте современный синтаксис ES6+
- Предпочитайте `const` и `let` вместо `var`
- Используйте стрелочные функции там, где это уместно
- Избегайте глобальных переменных

## 🎨 Стиль кода

### Форматирование

- Используйте 2 пробела для отступов
- Максимальная длина строки: 100 символов
- Добавляйте пустую строку между логическими блоками

### Именование

- **Компоненты**: PascalCase (`UserProfile`, `CalculatorForm`)
- **Функции**: camelCase (`calculateFee`, `validateForm`)
- **Переменные**: camelCase (`userName`, `isLoading`)
- **Константы**: UPPER_SNAKE_CASE (`MAX_RETRY_COUNT`)
- **Файлы**: kebab-case (`user-profile.jsx`, `calculator-form.tsx`)

### Импорты

```javascript
// Правильно
import React, { useState, useEffect } from 'react';
import { calculateFee } from '../utils/calculations';
import CalculatorForm from './CalculatorForm';

// Неправильно
import * as React from 'react';
import {calculateFee} from '../utils/calculations';
```

## 🧪 Тестирование

### Модульные тесты

- Добавляйте тесты для новых функций
- Тестируйте edge cases и ошибочные сценарии
- Используйте описательные названия тестов

```javascript
describe('Calculator', () => {
  test('должен правильно рассчитывать членский взнос для уровня 90', () => {
    // тестовая логика
  });
});
```

### E2E тесты

- Тестируйте критические пользовательские сценарии
- Проверяйте работу форм и навигации

## 📚 Документация

### Комментарии в коде

```javascript
// Правильно
/**
 * Рассчитывает размер членского взноса на основе уровня ответственности
 * @param {string} level - Уровень ответственности ('90', '500', '3e3', 'unlimited')
 * @returns {number} Размер взноса в рублях
 */
function calculateMembershipFee(level) {
  // Логика расчета
}

// Неправильно
// Функция для расчета
function calc(level) {
  // код
}
```

### Документация компонентов

```javascript
/**
 * Компонент калькулятора взносов СРО
 *
 * @param {Object} props
 * @param {string} props.selectedLevel - Выбранный уровень ответственности
 * @param {Function} props.onLevelChange - Обработчик изменения уровня
 */
function Calculator({ selectedLevel, onLevelChange }) {
  // реализация
}
```

## 🚨 Сообщение о проблемах

Если вы нашли ошибку или у вас есть предложение по улучшению:

1. Проверьте существующие Issues
2. Создайте новый Issue с подробным описанием
3. Укажите шаги для воспроизведения (если применимо)
4. Добавьте скриншоты или примеры кода

## 📞 Контакты

- **Email**: info@noso.ru
- **Issues**: [GitHub Issues](https://github.com/todmiv/sro/issues)

## 🙏 Благодарность

Спасибо за ваш вклад в развитие проекта НОСО! Ваша помощь делает наше сообщество сильнее.
